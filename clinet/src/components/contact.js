export function Contact(){
    return(
        <div id="contact-page">
            Here's where the form will go!
            get their name, email, and message.
            email has to be valid and message can't be blank
        </div>
    )
}